#ifndef ENUMS_H
#define ENUMS_H

enum class AbilityType {NormalAttack, Stun, Poison, Defend};
enum class ResistanceType {None, Physical, Magical};
enum class Difficulty {Easy, Medium, Hard};
enum class GameState {MainMenu, SaveMenu, LoadMenu, HubWorld, BeforeFight, Battle, Inventory, GameOver, EndGame, PopupResult};
#endif 

